{
  "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Testbed",
            "type": "shell",
            "command": "bazel build --compilation_mode=dbg //testbed:Testbed",
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ]
}